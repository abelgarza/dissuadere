from setuptools import setup, find_packages

setup(
    name='dissuadere',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Lista de dependencias necesarias para tu paquete
    ],
)